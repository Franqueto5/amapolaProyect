# amapolaProyect

Demostración de página web que organiza archivos de una base de datos y permite enviarlos a través de un puerto serie utilizando la API Web Serial. Abra `index.html` en un navegador compatible para probarla.
